{
  "compilerOptions": {
    // Basic
    "module": "commonjs",
    "target": "es6",

    // Output
    "outDir": "dist",
    // Dev Only
    "sourceMap": true,

    // Lib
    "lib": ["es6"],

    // Strictness
    "strict": true,
    "noUnusedLocals": true,
  }
}
